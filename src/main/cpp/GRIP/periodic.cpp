#include <stdio.h>
//#Include <opencv/networktables.h?


int main() {

    NetworkTable *table;

    table = NetworkTable::initialise("IP")
    table = NetworkTable::getDatabase("database");



    while (1) {
        //post variables to NT
    }

    return 0
}