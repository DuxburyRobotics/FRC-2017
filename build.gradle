group 'com.duxburyrobotics.frc-2017'
version '0.1'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
    flatDir {
        dir "libs"
    }
}

dependencies {
    compile name: "NetworkTables"
    compile name: "WPILib"
}
